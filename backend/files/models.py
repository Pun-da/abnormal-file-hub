# Models have been moved to contracts app (shared data contract)
# Import from contracts.models instead:
#   from contracts.models import File, FileContent

from contracts.models import File, FileContent

__all__ = ['File', 'FileContent']
